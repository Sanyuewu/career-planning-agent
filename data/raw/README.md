# data/raw

本目录用于本地放置原始或预处理招聘数据，例如：

- `preprocessed_for_llm.csv`
- `20260226105856_457.xls`

这些文件可能包含受授权限制的数据，默认不提交到公开仓库，也不打进 Docker 镜像。

运行完整市场数据、岗位真实样本或图谱重建流程前，请在本地按 README 准备同名数据文件；没有这些文件时，核心后端仍可启动，但依赖真实招聘样本的接口会降级为空结果。
