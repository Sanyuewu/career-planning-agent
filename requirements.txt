# ── Web 框架 ──────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9      # 简历文件上传

# ── 数据校验 & 配置 ────────────────────────────────────────
pydantic>=2.10.0
pydantic-settings>=2.6.0

# ── 认证 ──────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0   # JWT（含 cryptography → PII 字段加密复用）
bcrypt>=4.0.0                      # 密码哈希（替代弱 sha256）

# ── HTTP 客户端 ────────────────────────────────────────────
httpx>=0.28.0
requests>=2.32.0
openai>=1.0.0                # LLM API（DeepSeek / Qwen 等 OpenAI 兼容接口）

# ── 图谱 & 数据处理 ───────────────────────────────────────
networkx>=3.4

# ── YOUTU-GraphRAG 向量检索 ────────────────────────────────
torch>=2.0.0
transformers>=4.40.0
sentence-transformers>=2.3.0
faiss-cpu>=1.7.4
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0

# ── 文本处理（YOUTU-GraphRAG 内部） ───────────────────────
tiktoken>=0.5.0
json-repair>=0.10.0
PyYAML>=6.0
python-dotenv>=1.0.0
chardet>=5.0.0
nanoid>=2.0.0

# ── 简历解析 ───────────────────────────────────────────────
PyMuPDF>=1.24.0              # PDF 解析
python-docx>=1.1.0           # DOCX 解析

# ── 报告导出 ───────────────────────────────────────────────
reportlab>=4.0.0             # PDF 导出（可选，缺失时跳过）

# ── 持久化 & 迁移 ──────────────────────────────────────────
sqlalchemy>=2.0.0
aiosqlite>=0.19.0            # SQLite 异步驱动（开发默认）
alembic>=1.13               # 数据库 schema 迁移（prod：alembic upgrade head）

# ── 可选基础设施（未配置则自动降级，不影响单进程基线）────────
redis>=5.0                  # 缓存/分布式锁/限流（仅 REDIS_URL 配置时启用）
asyncpg>=0.29               # PostgreSQL 异步驱动（仅生产 Postgres 时需要）
